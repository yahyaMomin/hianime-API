{
  "name": "hianime",
  "scripts": {
    "build": "bun build index.js --outdir=dist --minify",
    "start": "bun run dist/index.js",
    "dev": "bun --watch run index.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "hono": "^4.6.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.24.0"
  }
}
